This repo provides a container that has:
 * The OpenPolicyAgent pam_authz module
 * An ssh client supporting u2f keys for authentication.
 * An ssh server configured to require both u2f and pam_authz 
